/*

passo 1: dentro da pasta que quero criar o repositório local, no terminal, 'git init'

passo 2: pra adicionar todos os arquivos 'git add .' ou pra adicionar um arquivo específico 'git add nomedoarquivo.extensão'

passo 3: criar ponto de restauração com comentário, autor e data 'git commit'. pra incluir a mensagem 'git commit -m "descrição da atualização feita"

passo 4: pra desfazer um commmit errado 'git commit -m "descrição utilizada no commit errado" -- amend'

! pra excluir o repositório git, posso ou apagar a pasta oculta '.git' ou usar o comando 'rm -rf .git' na pasta em que o repositório git foi criado

*/