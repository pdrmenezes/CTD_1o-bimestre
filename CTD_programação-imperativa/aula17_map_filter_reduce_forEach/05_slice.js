// .slice() retorna os elementos selecionados de uma matriz

// sintaxe: array.slice(inicio,fim)

const frutas = ['banana', 'maçã', 'pera', 'uva', 'laranja', 'limão', 'manga'];

const frutasCitricas = frutas.slice(4, 6);

console.log(frutasCitricas);