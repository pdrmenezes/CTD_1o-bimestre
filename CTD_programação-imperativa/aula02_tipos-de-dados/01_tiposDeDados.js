// variáveis:

// number
let idade = 25 // int
let preco = 15.25 // float
// os decimais em JS sãa escritos com ponto

// string: palavras ou frases sem limite de caracteres, pode conter números e letras
let nome = 'pedro menezes';
// seu nome precisa ser apresentado entre aspas sempre e tudo que estiver entre as aspas será interpretado como string

// boolean: verdadeiro ou falso
let nasceuNoBrasil = true;
// booleans não recebem aspas, senão viram strings

 // NaN = not a number
  'a' * 2 // NaN

 // null = valor vazio 
 let passageirosNoTrem = null;

 // undefined = variável que nunca teve valor atribuído a ela. mas reserva o espaço na memória pra ela e pode ser definido mais adiante

 let pessoas = undefined;
 // ou
 let pessoas;
 console.log(pessoas);
 espectadores = 10;
 console.log(pessoas);

 // objeto literal cria um conjunto de informações dentro de uma variável

 let minhaMoto = {
     marca : 'honda',
     anoFabricacao : 2016,
     modelo : 'pcx',
 }
  
console.log(minhaMoto)

 // arrays = listas, diferente dos objetos literais, usamos colchetes [] ao invés de chaves {}. os valores não possuem várias propriedades mas possuem índices numéricos ou indexes, referentes à sua posição na lista. A contagem começa do zero

 let seriesFavoritas =['friends', 'game of thrones', 'modern love'];
 console.log(seriesFavoritas)

 