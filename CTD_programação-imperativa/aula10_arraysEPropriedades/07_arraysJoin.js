// Join(): junta todos os elementos de uma array e os retorna como uma string
// podemos definir o separador dos elementos. se não definirmos, por padrão, as vírgulas são usadas

let colors = ["blue", "white", "red", "green"];
console.log(colors.join());

// substituindo o separador
console.log(colors.join(" - "));